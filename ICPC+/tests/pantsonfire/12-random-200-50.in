200 200
nb are worse than la
nb are worse than qa
gb are worse than xa
aa are worse than va
mb are worse than wa
aa are worse than ba
rb are worse than ob
bb are worse than ma
hb are worse than sa
ya are worse than tb
ab are worse than ca
na are worse than ib
kb are worse than vb
wb are worse than ib
va are worse than wb
mb are worse than na
ja are worse than bb
fb are worse than pb
da are worse than fb
oa are worse than pb
ga are worse than qa
sb are worse than xb
ga are worse than wa
la are worse than pb
ob are worse than lb
kb are worse than sa
nb are worse than ba
fa are worse than bb
xb are worse than da
la are worse than wa
lb are worse than ma
eb are worse than ta
fa are worse than hb
ca are worse than va
ab are worse than ia
lb are worse than va
ha are worse than rb
rb are worse than ua
ha are worse than xa
da are worse than wb
bb are worse than ia
za are worse than cb
ma are worse than kb
aa are worse than pa
nb are worse than mb
cb are worse than ua
ja are worse than jb
da are worse than la
ha are worse than eb
ua are worse than sb
da are worse than db
ob are worse than ub
ca are worse than bb
hb are worse than ja
aa are worse than kb
ia are worse than db
vb are worse than jb
ha are worse than ja
ya are worse than ka
ya are worse than na
aa are worse than ea
ob are worse than nb
ua are worse than xb
va are worse than qb
rb are worse than ab
ya are worse than jb
wa are worse than ab
jb are worse than pa
rb are worse than tb
sb are worse than ob
fb are worse than ca
oa are worse than cb
gb are worse than ga
xa are worse than ya
vb are worse than tb
xa are worse than ua
mb are worse than ta
ea are worse than ua
ob are worse than ta
xa are worse than eb
na are worse than ga
pa are worse than wb
tb are worse than jb
da are worse than qb
fb are worse than db
oa are worse than va
gb are worse than za
xa are worse than la
ea are worse than ja
ib are worse than db
ob are worse than vb
lb are worse than ya
na are worse than ca
za are worse than ga
xa are worse than mb
eb are worse than ga
ra are worse than ub
bb are worse than ra
ja are worse than ma
nb are worse than xb
fa are worse than rb
xa are worse than ba
mb are worse than ka
bb are worse than ba
qb are worse than pa
hb are worse than ya
xa are worse than ca
oa are worse than wb
za are worse than fa
wb are worse than ka
na are worse than la
na are worse than da
ga are worse than ca
fb are worse than eb
kb are worse than tb
cb are worse than la
aa are worse than eb
ub are worse than ba
vb are worse than ub
ya are worse than wb
cb are worse than da
da are worse than ja
nb are worse than ma
ba are worse than ib
na are worse than va
aa are worse than ra
va are worse than ib
la are worse than ma
xb are worse than jb
ka are worse than qa
rb are worse than pb
ta are worse than wb
aa are worse than ma
ya are worse than ib
xb are worse than ma
xa are worse than db
hb are worse than ka
ab are worse than qb
lb are worse than wb
ob are worse than pb
ia are worse than ka
xa are worse than ja
la are worse than va
rb are worse than lb
ca are worse than pb
na are worse than ba
na are worse than tb
hb are worse than ma
pb are worse than ma
ha are worse than cb
ma are worse than vb
mb are worse than da
ob are worse than ab
na are worse than qa
hb are worse than jb
fa are worse than la
da are worse than bb
la are worse than ub
gb are worse than xb
rb are worse than da
ob are worse than ga
ya are worse than ma
fb are worse than tb
ma are worse than ta
eb are worse than kb
hb are worse than fb
ob are worse than jb
xb are worse than bb
ia are worse than qb
jb are worse than ub
tb are worse than ka
ka are worse than db
fa are worse than ma
ma are worse than jb
aa are worse than ib
da are worse than ub
fa are worse than ba
cb are worse than ib
fb are worse than ub
za are worse than rb
za are worse than fb
sa are worse than ia
xa are worse than hb
fb are worse than ja
ea are worse than ha
aa are worse than za
oa are worse than ia
ra are worse than pb
xb are worse than sa
cb are worse than ba
ja are worse than ca
ea are worse than pa
eb are worse than la
ua are worse than ub
eb are worse than ja
oa are worse than gb
sb are worse than fb
cb are worse than ta
nb are worse than kb
fa are worse than nb
qb are worse than qa
rb are worse than ub
fb are worse than fa
ba are worse than db
ra are worse than fb
qb are worse than nb
db are worse than mb
da are worse than ka
db are worse than ta
sb are worse than xa
ca are worse than za
sb are worse than qb
la are worse than fa
fb are worse than sa
ub are worse than xa
ib are worse than nb
wa are worse than ra
sb are worse than xa
vb are worse than na
ga are worse than nb
da are worse than lb
db are worse than fb
ha are worse than ib
qb are worse than mb
da are worse than mb
na are worse than ja
wb are worse than za
oa are worse than fb
tb are worse than ma
ga are worse than eb
ca are worse than cb
vb are worse than wb
ma are worse than la
mb are worse than pa
ba are worse than ta
ea are worse than qb
wb are worse than ja
fb are worse than na
pa are worse than ub
wa are worse than pa
xa are worse than nb
wb are worse than ta
ba are worse than fa
xb are worse than fa
jb are worse than wb
ga are worse than pb
vb are worse than sa
ea are worse than jb
jb are worse than fa
ea are worse than hb
pa are worse than ka
cb are worse than pa
ua are worse than la
cb are worse than lb
ha are worse than da
fa are worse than fb
qb are worse than da
aa are worse than tb
ka are worse than ba
ba are worse than wb
xb are worse than ha
pb are worse than wb
bb are worse than tb
hb are worse than oa
da are worse than ob
ib are worse than wa
mb are worse than oa
ma are worse than ub
bb are worse than ja
wa are worse than db
ha are worse than jb
eb are worse than ka
jb are worse than fb
ra are worse than ab
eb are worse than ub
xa are worse than pa
ka are worse than ab
fa are worse than ja
vb are worse than ka
nb are worse than hb
ib are worse than bb
za are worse than pa
pa are worse than ea
ab are worse than ob
za are worse than pa
rb are worse than ma
vb are worse than ma
mb are worse than db
ka are worse than na
sb are worse than wa
ob are worse than ca
gb are worse than lb
ma are worse than va
ua are worse than wb
aa are worse than ta
vb are worse than ab
kb are worse than ma
eb are worse than na
ea are worse than qa
wb are worse than fb
hb are worse than sb
rb are worse than eb
ia are worse than fb
la are worse than ta
tb are worse than ia
ka are worse than za
ab are worse than ya
wb are worse than ub
ya are worse than ba
ra are worse than va
bb are worse than ea
ib are worse than ja
ta are worse than ob
wa are worse than la
na are worse than pa
rb are worse than ya
xb are worse than fa
tb are worse than sb
na are worse than mb
hb are worse than za
oa are worse than ja
ba are worse than oa
ha are worse than za
pa are worse than ya
vb are worse than sb
cb are worse than hb
ga are worse than vb
za are worse than hb
ra are worse than vb
ub are worse than eb
na are worse than ob
ga are worse than ma
vb are worse than ba
ia are worse than ma
ob are worse than rb
lb are worse than ga
ca are worse than ob
wa are worse than ja
ra are worse than mb
jb are worse than qa
ib are worse than oa
ba are worse than ob
la are worse than bb
ya are worse than eb
ya are worse than ua
na are worse than nb
ba are worse than ja
va are worse than la
va are worse than na
ea are worse than ka
ta are worse than ia
aa are worse than pb
ta are worse than sa
fb are worse than fa
ib are worse than qa
ta are worse than kb
ga are worse than gb
pa are worse than gb
na are worse than nb
fa are worse than cb
db are worse than ra
vb are worse than gb
bb are worse than sa
ub are worse than aa
la are worse than ca
ub are worse than gb
fb are worse than bb
lb are worse than sa
rb are worse than ia
pb are worse than va
ib are worse than ga
ma are worse than nb
ib are worse than pb
ha are worse than hb
ab are worse than kb
ea are worse than wa
kb are worse than jb
ya are worse than vb
la are worse than kb
wa are worse than sb
na are worse than ja
ma are worse than ja
pb are worse than tb
ta are worse than ea
lb are worse than fa
mb are worse than xa
ib are worse than qb
tb are worse than ua
eb are worse than wa
la are worse than fb
ia are worse than hb
za are worse than xa
tb are worse than ob
gb are worse than pa
ka are worse than tb
cb are worse than ca
pb are worse than vb
ia are worse than eb
ca are worse than fa
db are worse than ma
