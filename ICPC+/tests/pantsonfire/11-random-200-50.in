200 200
kb are worse than ea
ca are worse than ma
fb are worse than hb
gb are worse than ma
sa are worse than aa
za are worse than ta
ea are worse than sb
ea are worse than xb
ta are worse than sb
da are worse than ga
ca are worse than eb
xa are worse than wb
ib are worse than qb
aa are worse than ub
la are worse than aa
bb are worse than wb
ga are worse than fb
ma are worse than lb
pa are worse than sb
ca are worse than hb
ub are worse than db
fa are worse than ca
xa are worse than la
da are worse than sb
da are worse than ka
mb are worse than ab
ka are worse than ya
ua are worse than na
wa are worse than ib
pa are worse than xb
ib are worse than va
fb are worse than ub
ia are worse than wb
qb are worse than ma
fb are worse than nb
pb are worse than mb
rb are worse than qa
aa are worse than vb
la are worse than ia
fa are worse than qa
ua are worse than ub
qb are worse than fb
ua are worse than pa
sb are worse than rb
xb are worse than ja
sb are worse than hb
eb are worse than bb
fb are worse than ob
za are worse than xa
fa are worse than ja
oa are worse than ub
vb are worse than ka
na are worse than xb
ha are worse than ab
ha are worse than oa
qb are worse than eb
ka are worse than kb
la are worse than fb
ba are worse than ha
ab are worse than aa
sa are worse than ua
jb are worse than aa
wb are worse than qa
qb are worse than tb
gb are worse than ia
sb are worse than gb
sa are worse than ja
fb are worse than bb
jb are worse than pb
qb are worse than db
va are worse than ca
pa are worse than ib
jb are worse than ra
pb are worse than la
ta are worse than ja
ja are worse than gb
xa are worse than ba
ha are worse than ra
xb are worse than qb
ab are worse than tb
ya are worse than ub
wa are worse than xa
wa are worse than lb
ha are worse than ia
ga are worse than aa
ta are worse than cb
la are worse than da
jb are worse than wa
cb are worse than bb
wa are worse than na
ca are worse than cb
wa are worse than cb
va are worse than xa
ub are worse than rb
ta are worse than wa
ob are worse than sb
ba are worse than db
ca are worse than xa
da are worse than ra
jb are worse than ab
va are worse than ob
fa are worse than ea
vb are worse than ja
ya are worse than db
mb are worse than ob
ia are worse than nb
va are worse than gb
la are worse than na
sa are worse than gb
mb are worse than ea
ja are worse than lb
na are worse than tb
fa are worse than ub
mb are worse than db
wa are worse than eb
hb are worse than qa
ca are worse than oa
va are worse than ba
ba are worse than qb
hb are worse than oa
wb are worse than oa
ab are worse than lb
sa are worse than jb
eb are worse than gb
na are worse than ga
wb are worse than lb
xa are worse than da
mb are worse than sb
ea are worse than nb
da are worse than xb
pb are worse than hb
fa are worse than ra
ra are worse than lb
ba are worse than aa
ga are worse than ia
ca are worse than tb
pa are worse than da
ra are worse than nb
ga are worse than ha
jb are worse than ka
vb are worse than ya
za are worse than rb
ga are worse than ob
ra are worse than rb
ab are worse than ob
tb are worse than oa
va are worse than qa
qb are worse than qa
ja are worse than fb
ua are worse than xa
ta are worse than ia
za are worse than tb
mb are worse than tb
mb are worse than ta
wb are worse than ma
ba are worse than ob
nb are worse than ma
la are worse than qb
ca are worse than gb
aa are worse than db
lb are worse than rb
ia are worse than lb
ha are worse than lb
db are worse than lb
xa are worse than tb
fa are worse than fb
ya are worse than ea
ba are worse than ya
za are worse than lb
gb are worse than tb
pb are worse than va
ca are worse than db
ra are worse than ab
aa are worse than eb
ca are worse than ob
vb are worse than ea
cb are worse than la
ga are worse than lb
sa are worse than xb
ib are worse than sb
ib are worse than kb
ya are worse than qb
ba are worse than ka
hb are worse than wb
ta are worse than qb
fa are worse than za
jb are worse than vb
ra are worse than vb
ba are worse than eb
za are worse than oa
wa are worse than ca
ib are worse than ga
ca are worse than ga
ba are worse than xb
za are worse than ia
xa are worse than ha
ab are worse than ya
la are worse than eb
jb are worse than na
ha are worse than sb
wb are worse than gb
ea are worse than mb
ga are worse than oa
da are worse than gb
mb are worse than ib
oa are worse than qb
rb are worse than ia
va are worse than ea
mb are worse than ia
ta are worse than bb
ub are worse than fb
pb are worse than sb
ta are worse than pa
aa are worse than za
ya are worse than ja
pb are worse than ab
ka are worse than xb
gb are worse than hb
aa are worse than wb
ga are worse than wb
xb are worse than ub
ga are worse than sa
xa are worse than ea
xb are worse than qa
ba are worse than va
gb are worse than da
wb are worse than ga
na are worse than ua
cb are worse than ga
wb are worse than nb
ga are worse than pb
ha are worse than ob
ya are worse than ha
mb are worse than ba
da are worse than ub
aa are worse than ca
sb are worse than ia
xa are worse than sa
pa are worse than ya
kb are worse than qa
tb are worse than sa
ab are worse than ib
qa are worse than ha
rb are worse than pa
ub are worse than xb
ia are worse than da
pb are worse than ib
ma are worse than fb
wb are worse than rb
rb are worse than na
ra are worse than eb
fb are worse than pa
wb are worse than aa
la are worse than za
lb are worse than bb
gb are worse than eb
sb are worse than eb
fb are worse than ea
mb are worse than xb
fa are worse than ab
ra are worse than ib
pa are worse than nb
ha are worse than na
ib are worse than ba
ab are worse than da
fb are worse than ja
ua are worse than fb
ma are worse than fb
ia are worse than xb
fb are worse than ha
ya are worse than ma
ma are worse than la
kb are worse than aa
la are worse than pb
cb are worse than eb
bb are worse than sb
ta are worse than ib
ha are worse than da
lb are worse than kb
ia are worse than ua
tb are worse than db
ca are worse than ha
ia are worse than xa
fa are worse than nb
fb are worse than db
ba are worse than rb
pb are worse than nb
tb are worse than ea
eb are worse than ya
ib are worse than ha
ua are worse than da
sb are worse than bb
ia are worse than hb
pa are worse than qa
ea are worse than na
vb are worse than ga
ua are worse than ia
sa are worse than fb
hb are worse than xa
ua are worse than oa
fa are worse than ya
ja are worse than ub
na are worse than ha
hb are worse than kb
fa are worse than da
pb are worse than xb
eb are worse than xb
la are worse than pa
tb are worse than hb
za are worse than pa
oa are worse than ka
nb are worse than jb
za are worse than ub
la are worse than kb
qb are worse than ya
rb are worse than sb
ua are worse than ab
gb are worse than ab
fb are worse than xa
bb are worse than oa
ya are worse than ma
ma are worse than rb
ba are worse than xa
ka are worse than ub
ga are worse than jb
oa are worse than xa
ub are worse than fa
ia are worse than xa
ba are worse than va
la are worse than tb
tb are worse than ha
pb are worse than qa
hb are worse than xa
sb are worse than ra
ub are worse than va
fb are worse than ua
ib are worse than rb
na are worse than lb
sa are worse than ya
ib are worse than ya
fa are worse than ha
cb are worse than ub
ba are worse than ab
da are worse than qb
sb are worse than mb
pa are worse than mb
fa are worse than na
la are worse than kb
jb are worse than tb
fb are worse than aa
hb are worse than xb
ab are worse than ta
xb are worse than ab
hb are worse than kb
gb are worse than na
ib are worse than jb
nb are worse than lb
bb are worse than hb
bb are worse than sb
xa are worse than jb
ja are worse than jb
xa are worse than sb
gb are worse than fa
ub are worse than ra
ea are worse than xa
mb are worse than gb
pa are worse than ja
ga are worse than qa
ca are worse than ra
ga are worse than kb
ca are worse than ub
za are worse than fb
fb are worse than da
bb are worse than ha
ob are worse than kb
qb are worse than na
gb are worse than ya
sa are worse than fb
qb are worse than za
ea are worse than eb
ha are worse than va
ob are worse than pa
lb are worse than za
ia are worse than qb
tb are worse than pa
cb are worse than xb
hb are worse than ub
la are worse than ka
da are worse than ba
vb are worse than tb
oa are worse than fb
wa are worse than jb
ma are worse than oa
ka are worse than sa
ub are worse than ta
nb are worse than mb
jb are worse than db
qa are worse than rb
va are worse than la
ka are worse than ba
