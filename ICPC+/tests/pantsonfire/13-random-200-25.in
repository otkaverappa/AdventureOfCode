200 200
aa are worse than ha
oa are worse than ga
qa are worse than ta
va are worse than pa
fa are worse than ba
da are worse than ga
xa are worse than da
sa are worse than ba
ra are worse than na
ta are worse than sa
wa are worse than ha
ka are worse than wa
ca are worse than ka
ea are worse than sa
ta are worse than xa
pa are worse than ua
ra are worse than sa
da are worse than pa
ya are worse than xa
la are worse than ta
ha are worse than ga
ca are worse than ba
aa are worse than wa
ta are worse than wa
va are worse than ia
ra are worse than da
ea are worse than la
ba are worse than va
qa are worse than sa
la are worse than wa
la are worse than ka
da are worse than oa
ea are worse than ia
ra are worse than ga
fa are worse than na
ua are worse than aa
wa are worse than ma
da are worse than ba
xa are worse than ga
ya are worse than ba
ya are worse than pa
ma are worse than ga
xa are worse than ja
ra are worse than pa
ea are worse than ba
ka are worse than ba
ea are worse than ha
na are worse than ja
ka are worse than na
qa are worse than ja
oa are worse than pa
ba are worse than aa
la are worse than ma
ca are worse than fa
ea are worse than ma
ga are worse than ia
oa are worse than ja
ia are worse than ja
la are worse than ba
ya are worse than wa
aa are worse than ja
ra are worse than ka
qa are worse than aa
ta are worse than ka
sa are worse than na
oa are worse than ia
da are worse than wa
fa are worse than ja
ta are worse than ra
la are worse than xa
ea are worse than ra
ya are worse than ga
na are worse than oa
sa are worse than ya
ya are worse than ca
va are worse than ua
ka are worse than pa
qa are worse than wa
ba are worse than ja
xa are worse than ha
qa are worse than ya
ra are worse than ya
ya are worse than fa
xa are worse than wa
ta are worse than ga
sa are worse than aa
ra are worse than ha
ya are worse than ja
ra are worse than ca
ba are worse than na
va are worse than ja
ma are worse than ja
ya are worse than na
ca are worse than ia
ra are worse than va
ra are worse than ja
oa are worse than wa
ka are worse than ma
qa are worse than ha
qa are worse than fa
pa are worse than wa
ea are worse than wa
da are worse than ca
ya are worse than ia
xa are worse than ma
na are worse than pa
aa are worse than ga
va are worse than na
ta are worse than aa
ya are worse than da
xa are worse than va
fa are worse than ia
sa are worse than ia
qa are worse than ua
da are worse than fa
qa are worse than na
fa are worse than ga
xa are worse than na
ca are worse than va
qa are worse than la
xa are worse than fa
xa are worse than ua
na are worse than ma
ca are worse than wa
da are worse than ka
la are worse than ha
fa are worse than ha
sa are worse than da
ca are worse than ha
na are worse than aa
ra are worse than aa
ta are worse than ca
ua are worse than ja
ea are worse than ja
fa are worse than wa
ya are worse than ha
qa are worse than ia
ya are worse than oa
qa are worse than pa
la are worse than va
qa are worse than ca
ha are worse than ja
qa are worse than ea
fa are worse than pa
sa are worse than ja
ea are worse than ya
ra are worse than ia
ca are worse than pa
ra are worse than xa
da are worse than ua
ra are worse than ma
ca are worse than oa
ua are worse than wa
wa are worse than ga
da are worse than ha
ta are worse than da
ya are worse than ua
sa are worse than xa
ba are worse than wa
ca are worse than ma
qa are worse than ma
ga are worse than ja
qa are worse than ra
ka are worse than oa
na are worse than ha
da are worse than na
ta are worse than ha
ba are worse than ua
wa are worse than ia
pa are worse than ja
qa are worse than ba
fa are worse than va
la are worse than ja
ba are worse than ma
ca are worse than aa
wa are worse than ja
ea are worse than ta
fa are worse than ma
oa are worse than ha
ra are worse than oa
ka are worse than fa
ua are worse than ia
ua are worse than ga
sa are worse than va
sa are worse than fa
ka are worse than va
ba are worse than pa
va are worse than ha
ea are worse than ca
ta are worse than ia
ha are worse than ma
ca are worse than ga
oa are worse than aa
sa are worse than ua
da are worse than ma
ya are worse than ma
la are worse than aa
sa are worse than ha
qa are worse than da
da are worse than va
ea are worse than aa
ga are worse than oa
na are worse than ya
ma are worse than sa
na are worse than ra
ia are worse than oa
ea are worse than ka
ba are worse than qa
ha are worse than sa
ha are worse than wa
va are worse than aa
fa are worse than ka
ja are worse than ma
aa are worse than sa
ta are worse than pa
ha are worse than ta
xa are worse than oa
ga are worse than aa
ha are worse than ra
ra are worse than qa
ma are worse than ya
ba are worse than ra
va are worse than qa
la are worse than ua
ua are worse than pa
ka are worse than ca
ra are worse than ta
va are worse than la
ka are worse than ia
ta are worse than oa
qa are worse than va
sa are worse than ka
ma are worse than fa
la are worse than da
sa are worse than ma
ya are worse than ta
sa are worse than ma
ba are worse than la
ha are worse than ca
ka are worse than ja
ga are worse than ua
ia are worse than ya
fa are worse than ua
ha are worse than sa
ca are worse than ta
ga are worse than fa
aa are worse than pa
ta are worse than ua
ea are worse than xa
ia are worse than la
ca are worse than sa
wa are worse than ca
fa are worse than ea
xa are worse than ca
ta are worse than va
ea are worse than pa
ka are worse than ga
ba are worse than la
na are worse than da
ca are worse than la
ka are worse than ja
xa are worse than ra
pa are worse than xa
ua are worse than la
ua are worse than pa
fa are worse than ea
na are worse than ia
ta are worse than ba
ga are worse than xa
aa are worse than ta
ha are worse than oa
oa are worse than ca
ea are worse than oa
ta are worse than va
na are worse than qa
ja are worse than ia
ga are worse than ba
ja are worse than oa
ha are worse than qa
ea are worse than ua
ia are worse than aa
sa are worse than ka
va are worse than wa
pa are worse than ea
oa are worse than ea
ia are worse than ua
sa are worse than ca
qa are worse than ga
va are worse than ca
fa are worse than ca
sa are worse than ma
sa are worse than wa
ja are worse than ta
fa are worse than da
da are worse than ia
fa are worse than qa
ga are worse than ta
wa are worse than oa
oa are worse than ra
ba are worse than qa
ma are worse than ra
va are worse than ra
ua are worse than qa
ha are worse than pa
wa are worse than ya
ja are worse than oa
ha are worse than ia
la are worse than ea
wa are worse than ua
ma are worse than aa
la are worse than ya
ta are worse than oa
ha are worse than pa
na are worse than qa
la are worse than ea
da are worse than ya
ea are worse than pa
aa are worse than xa
ya are worse than ta
pa are worse than aa
ia are worse than la
ja are worse than ca
oa are worse than ea
ba are worse than la
ua are worse than la
qa are worse than ga
ra are worse than wa
fa are worse than xa
wa are worse than pa
ra are worse than ea
sa are worse than ca
pa are worse than ea
sa are worse than oa
ba are worse than la
wa are worse than ua
ia are worse than ea
ma are worse than sa
la are worse than qa
aa are worse than ca
ba are worse than ta
ra are worse than ta
ja are worse than ua
pa are worse than qa
na are worse than la
ea are worse than ga
pa are worse than la
ia are worse than ha
va are worse than ba
wa are worse than oa
ja are worse than ea
ra are worse than ba
va are worse than fa
pa are worse than oa
ga are worse than ta
ua are worse than xa
ia are worse than la
ia are worse than qa
ba are worse than qa
ja are worse than da
ua are worse than ba
ja are worse than ea
oa are worse than ya
sa are worse than wa
ta are worse than pa
ua are worse than ca
ba are worse than fa
ua are worse than va
pa are worse than la
aa are worse than ya
fa are worse than ra
na are worse than ca
va are worse than ca
ia are worse than ca
ka are worse than ra
ga are worse than ba
ra are worse than ba
qa are worse than va
pa are worse than ra
na are worse than ia
ja are worse than da
ua are worse than la
ta are worse than la
ga are worse than ha
na are worse than ca
aa are worse than ia
na are worse than ba
xa are worse than ea
va are worse than ca
ca are worse than la
pa are worse than ia
xa are worse than ta
ga are worse than aa
ea are worse than na
oa are worse than la
wa are worse than pa
va are worse than ka
sa are worse than ta
ja are worse than ma
ta are worse than la
aa are worse than qa
