200 200
ob are worse than rb
kc are worse than xb
bd are worse than wa
hd are worse than zc
mc are worse than ac
va are worse than sb
va are worse than qd
qb are worse than da
zb are worse than od
gb are worse than ga
gb are worse than yb
pd are worse than vd
sa are worse than jd
na are worse than qa
rd are worse than kb
ia are worse than jd
cd are worse than wa
lb are worse than hc
sb are worse than wc
kd are worse than ja
ab are worse than kc
fa are worse than aa
ec are worse than vb
dc are worse than ia
od are worse than jd
oa are worse than yc
ld are worse than ha
xc are worse than ia
vb are worse than kd
ya are worse than qb
zc are worse than gb
eb are worse than fc
ga are worse than pc
qc are worse than yb
pb are worse than fc
la are worse than ta
ta are worse than qa
tc are worse than xc
qa are worse than ab
jb are worse than ic
oa are worse than xb
kb are worse than da
ja are worse than ma
lc are worse than ec
ac are worse than jd
yb are worse than db
tc are worse than gb
mb are worse than db
vd are worse than ha
cd are worse than od
mc are worse than nd
mc are worse than jb
kc are worse than cb
kc are worse than ud
kd are worse than cb
vd are worse than yc
fd are worse than ic
ob are worse than vb
qa are worse than pd
id are worse than ta
wc are worse than ka
zb are worse than na
nb are worse than da
ua are worse than tc
pa are worse than la
xa are worse than bb
vb are worse than lb
dc are worse than jc
jb are worse than rd
pb are worse than jd
tc are worse than ya
vb are worse than mc
xa are worse than qa
ja are worse than kc
jb are worse than fd
ya are worse than mb
wb are worse than pa
pb are worse than ua
jd are worse than ld
ec are worse than ca
aa are worse than fb
dd are worse than vd
rc are worse than kb
fb are worse than bc
rb are worse than cc
ad are worse than rb
sa are worse than bc
hc are worse than bb
pa are worse than ub
lc are worse than nd
gd are worse than la
ha are worse than ab
gd are worse than qd
pa are worse than od
ya are worse than db
pc are worse than pd
ra are worse than bd
ba are worse than aa
hb are worse than tb
cc are worse than la
kd are worse than za
mc are worse than vd
ac are worse than gb
tb are worse than pc
xa are worse than db
fb are worse than ed
dc are worse than pa
yc are worse than wa
ha are worse than yc
md are worse than fd
kb are worse than eb
ab are worse than rc
bb are worse than dd
gb are worse than pc
ga are worse than xb
oc are worse than ka
od are worse than jc
nb are worse than aa
sd are worse than mc
tc are worse than fa
va are worse than vd
gb are worse than gc
dd are worse than gc
bd are worse than ia
nd are worse than ma
bb are worse than xb
jc are worse than pd
ma are worse than qd
ec are worse than qb
ca are worse than ba
dc are worse than pb
bc are worse than fd
ja are worse than xc
sc are worse than fc
od are worse than pd
pd are worse than ub
xa are worse than wc
jb are worse than aa
pc are worse than kc
yb are worse than cd
ia are worse than oa
ec are worse than cb
ra are worse than la
ub are worse than fb
kd are worse than md
kc are worse than ca
dc are worse than ha
ba are worse than fb
hb are worse than gc
id are worse than qc
gc are worse than fa
hd are worse than nd
wc are worse than la
ha are worse than eb
nb are worse than zc
vd are worse than nd
od are worse than xb
jd are worse than vd
dd are worse than kd
eb are worse than oa
va are worse than sa
ra are worse than gc
jc are worse than rb
ab are worse than ic
wa are worse than xb
lc are worse than oc
yb are worse than xc
zb are worse than qb
xb are worse than mb
pb are worse than eb
sd are worse than yc
nb are worse than fb
gb are worse than ic
sc are worse than oa
td are worse than nb
jb are worse than xa
zb are worse than gd
qd are worse than vc
hd are worse than ja
ob are worse than kb
gd are worse than nd
pa are worse than hc
jb are worse than hd
vb are worse than ta
td are worse than xb
va are worse than pc
lc are worse than sa
gc are worse than yb
pd are worse than ja
fd are worse than uc
td are worse than xc
sa are worse than qb
pc are worse than zb
nc are worse than ib
ka are worse than la
sd are worse than ub
ma are worse than kc
ha are worse than sc
na are worse than aa
ob are worse than pa
yc are worse than lb
md are worse than lb
sc are worse than ic
fb are worse than ld
la are worse than td
yc are worse than vc
ia are worse than wc
uc are worse than id
ca are worse than ta
yc are worse than pa
ed are worse than fd
hd are worse than vb
kb are worse than ra
gb are worse than xc
nc are worse than wc
mc are worse than jd
ib are worse than sd
db are worse than td
tb are worse than fa
sc are worse than fa
bd are worse than ob
kc are worse than ac
bb are worse than ra
xa are worse than dc
gb are worse than fa
mb are worse than rc
ac are worse than ab
jd are worse than da
ca are worse than tc
bd are worse than ta
ub are worse than rd
ha are worse than cd
rb are worse than kb
eb are worse than kd
pb are worse than aa
qd are worse than uc
pd are worse than ia
yc are worse than uc
za are worse than aa
bb are worse than ja
fa are worse than ra
bd are worse than ka
sb are worse than sa
na are worse than ub
pb are worse than sb
od are worse than zc
ua are worse than jb
na are worse than yb
yc are worse than ga
ca are worse than sc
eb are worse than rd
ad are worse than ha
ta are worse than ec
hc are worse than ub
nc are worse than qa
qa are worse than sd
oc are worse than od
gc are worse than ia
kd are worse than yc
rd are worse than cd
bc are worse than pa
qc are worse than sa
ra are worse than sb
yc are worse than ua
oc are worse than ha
pc are worse than sb
oc are worse than nb
ac are worse than zc
hd are worse than sa
pd are worse than ba
oc are worse than ua
na are worse than hb
mc are worse than uc
pb are worse than ea
aa are worse than kd
lb are worse than cc
ec are worse than fa
ya are worse than za
xb are worse than ba
tb are worse than pa
ic are worse than nc
la are worse than ad
ea are worse than qa
id are worse than wb
rd are worse than ac
ua are worse than ia
lb are worse than gd
gb are worse than eb
pb are worse than hb
ta are worse than ad
nb are worse than bb
fd are worse than lc
ac are worse than gd
qa are worse than yb
zb are worse than yc
pa are worse than oa
ga are worse than tc
eb are worse than jc
mc are worse than qa
kc are worse than hc
va are worse than ed
ua are worse than gc
ta are worse than ac
ld are worse than ic
lb are worse than ic
mb are worse than yc
jd are worse than vc
od are worse than nb
fc are worse than ia
ld are worse than ic
hd are worse than ec
wc are worse than nd
bd are worse than fc
fa are worse than sb
za are worse than pc
dc are worse than ub
ba are worse than xc
fa are worse than ua
cd are worse than ub
gb are worse than rc
ga are worse than sc
ub are worse than tb
rb are worse than mb
gd are worse than ac
ud are worse than ab
ma are worse than fd
ib are worse than jd
bb are worse than oc
sb are worse than gd
ka are worse than va
da are worse than nc
qc are worse than ea
pd are worse than za
zc are worse than za
tc are worse than ja
qd are worse than db
wc are worse than xc
ed are worse than ka
cb are worse than dd
kd are worse than od
qa are worse than xc
bd are worse than rd
pb are worse than xc
bd are worse than zb
xb are worse than ib
qc are worse than ra
nb are worse than jc
qb are worse than qa
mb are worse than nc
za are worse than hb
ub are worse than fc
za are worse than ua
hd are worse than ic
ed are worse than na
pb are worse than tc
fc are worse than ja
ub are worse than ld
fa are worse than nb
qa are worse than kb
md are worse than nc
tc are worse than ka
ed are worse than kc
ya are worse than yc
ad are worse than kc
pd are worse than dc
rd are worse than nb
na are worse than vc
la are worse than ud
hb are worse than qd
lc are worse than ab
ra are worse than uc
jb are worse than pa
jc are worse than od
cc are worse than ad
db are worse than sd
rb are worse than aa
ga are worse than ad
kc are worse than eb
cc are worse than od
pd are worse than wb
vb are worse than ob
nb are worse than ma
nc are worse than zb
tb are worse than zc
yc are worse than oa
yb are worse than ma
db are worse than lc
ea are worse than fd
pb are worse than hd
fb are worse than hd
pa are worse than zb
ua are worse than rc
rb are worse than nc
rc are worse than rb
fa are worse than ja
ha are worse than tc
ic are worse than wa
va are worse than fc
vd are worse than ud
tc are worse than ba
