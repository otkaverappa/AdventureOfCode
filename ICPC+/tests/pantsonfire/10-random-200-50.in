200 200
qa are worse than db
ba are worse than za
ua are worse than eb
ma are worse than ka
ua are worse than va
wa are worse than ua
hb are worse than xb
kb are worse than ka
tb are worse than aa
ob are worse than mb
nb are worse than pb
vb are worse than sb
rb are worse than hb
ja are worse than kb
ab are worse than cb
fb are worse than tb
ka are worse than gb
ma are worse than ib
ob are worse than ib
ga are worse than oa
ga are worse than pb
qa are worse than rb
ea are worse than na
rb are worse than ub
da are worse than gb
lb are worse than ba
ab are worse than fb
kb are worse than lb
ta are worse than oa
qa are worse than ba
la are worse than ab
hb are worse than gb
ia are worse than ub
sa are worse than aa
gb are worse than ab
qa are worse than nb
sa are worse than gb
gb are worse than ya
vb are worse than za
ua are worse than ba
ka are worse than ia
ea are worse than mb
pb are worse than ob
ga are worse than sb
la are worse than sa
ua are worse than tb
ca are worse than eb
rb are worse than ua
gb are worse than za
vb are worse than wb
oa are worse than ra
hb are worse than wb
rb are worse than eb
ya are worse than na
xa are worse than jb
ya are worse than oa
bb are worse than aa
bb are worse than ib
sb are worse than mb
ea are worse than qa
jb are worse than ga
wb are worse than eb
cb are worse than eb
ia are worse than za
db are worse than ya
sb are worse than ma
xa are worse than lb
qb are worse than xb
da are worse than ya
jb are worse than ab
rb are worse than lb
ma are worse than ua
xa are worse than ia
ka are worse than bb
za are worse than wb
hb are worse than za
ga are worse than mb
nb are worse than jb
pa are worse than jb
xb are worse than mb
nb are worse than cb
ga are worse than db
jb are worse than xb
sa are worse than xb
ia are worse than gb
gb are worse than fa
da are worse than ja
ia are worse than ua
wb are worse than xb
aa are worse than ra
xa are worse than ga
ma are worse than db
ja are worse than ua
na are worse than fb
ha are worse than tb
kb are worse than ta
ka are worse than ha
jb are worse than na
jb are worse than aa
sb are worse than sa
fb are worse than ca
kb are worse than rb
fb are worse than wb
pa are worse than tb
db are worse than wa
qb are worse than sa
bb are worse than gb
bb are worse than ia
ja are worse than db
da are worse than sb
fb are worse than xb
sa are worse than ya
ta are worse than aa
ab are worse than ha
ga are worse than lb
ha are worse than db
lb are worse than ob
wa are worse than pb
qb are worse than pa
pb are worse than cb
na are worse than ra
jb are worse than ia
ba are worse than ib
oa are worse than va
la are worse than ca
vb are worse than da
ma are worse than ia
ca are worse than ba
ja are worse than ya
ja are worse than ia
fb are worse than lb
ub are worse than la
vb are worse than nb
pb are worse than oa
bb are worse than sa
ta are worse than za
fa are worse than eb
hb are worse than ob
bb are worse than lb
lb are worse than za
ca are worse than cb
nb are worse than pa
ka are worse than za
pb are worse than ua
ta are worse than sa
pb are worse than tb
vb are worse than gb
ka are worse than ab
nb are worse than la
ma are worse than rb
nb are worse than ba
ia are worse than wa
ka are worse than sa
vb are worse than ib
ab are worse than pb
ga are worse than wb
ja are worse than ub
jb are worse than la
ha are worse than wb
ua are worse than xb
rb are worse than ra
ja are worse than gb
xa are worse than ma
kb are worse than ib
vb are worse than sa
va are worse than ra
ta are worse than pa
ua are worse than ib
ma are worse than la
ra are worse than ca
rb are worse than db
na are worse than ba
xa are worse than bb
ea are worse than wa
rb are worse than cb
ua are worse than na
oa are worse than ib
nb are worse than oa
ra are worse than eb
ub are worse than hb
la are worse than va
rb are worse than ha
qb are worse than jb
ya are worse than lb
qa are worse than ub
aa are worse than eb
pa are worse than rb
la are worse than cb
sa are worse than cb
la are worse than oa
qa are worse than ha
ta are worse than gb
da are worse than wa
pa are worse than ib
pb are worse than ca
ma are worse than fb
qa are worse than gb
ta are worse than hb
nb are worse than mb
ma are worse than gb
db are worse than oa
ra are worse than qa
pb are worse than ia
sa are worse than na
qb are worse than hb
tb are worse than ga
oa are worse than ia
eb are worse than oa
jb are worse than cb
aa are worse than ib
oa are worse than qa
qa are worse than jb
ba are worse than ha
ab are worse than va
aa are worse than mb
fa are worse than db
gb are worse than pa
wb are worse than ga
hb are worse than oa
oa are worse than pa
db are worse than xb
tb are worse than fb
kb are worse than la
vb are worse than ea
vb are worse than ba
ma are worse than xa
wa are worse than oa
na are worse than db
tb are worse than sa
ca are worse than ob
pa are worse than kb
ha are worse than ab
na are worse than ha
gb are worse than ub
va are worse than xb
aa are worse than ga
hb are worse than mb
ra are worse than hb
rb are worse than qa
kb are worse than fa
nb are worse than ka
va are worse than fa
nb are worse than qa
za are worse than rb
ia are worse than ob
eb are worse than pa
ra are worse than rb
wa are worse than ba
nb are worse than tb
wb are worse than fa
ia are worse than hb
rb are worse than bb
sa are worse than kb
sb are worse than ea
rb are worse than jb
ob are worse than ea
eb are worse than ra
vb are worse than ob
ia are worse than db
fa are worse than bb
wa are worse than db
eb are worse than la
bb are worse than jb
na are worse than bb
ob are worse than xa
ma are worse than hb
ba are worse than fb
ua are worse than vb
jb are worse than pa
gb are worse than ca
eb are worse than za
ia are worse than ma
qb are worse than ha
fa are worse than ca
lb are worse than ma
fb are worse than ja
qb are worse than oa
xa are worse than va
xb are worse than kb
wb are worse than nb
wa are worse than na
rb are worse than sb
ma are worse than sb
hb are worse than sb
va are worse than pb
ia are worse than qa
wb are worse than na
ua are worse than jb
ab are worse than ea
tb are worse than ua
pb are worse than ta
da are worse than ib
hb are worse than vb
db are worse than vb
cb are worse than rb
sb are worse than la
lb are worse than pa
ca are worse than tb
hb are worse than aa
ja are worse than ea
mb are worse than jb
ka are worse than jb
fa are worse than wb
xb are worse than la
nb are worse than ja
gb are worse than qb
pb are worse than ya
pa are worse than ta
ra are worse than ja
eb are worse than ea
ab are worse than gb
ca are worse than ka
qa are worse than xa
ub are worse than cb
ka are worse than da
ia are worse than pa
ba are worse than ha
cb are worse than qb
sa are worse than ka
kb are worse than na
eb are worse than cb
ob are worse than vb
vb are worse than ja
ia are worse than eb
qa are worse than ib
oa are worse than sa
ub are worse than xb
db are worse than oa
ya are worse than bb
qb are worse than kb
eb are worse than ca
ba are worse than hb
la are worse than eb
za are worse than db
qb are worse than la
tb are worse than wa
pb are worse than jb
va are worse than pb
sb are worse than db
va are worse than ka
qa are worse than jb
cb are worse than oa
lb are worse than ja
db are worse than na
kb are worse than xa
aa are worse than sa
cb are worse than ba
ba are worse than nb
fb are worse than cb
ta are worse than ab
ob are worse than qa
wb are worse than hb
ba are worse than ua
ya are worse than vb
ba are worse than tb
ka are worse than ra
gb are worse than ua
aa are worse than na
hb are worse than mb
kb are worse than jb
ha are worse than oa
nb are worse than ia
ga are worse than wa
lb are worse than bb
wb are worse than db
rb are worse than xb
ma are worse than sb
ta are worse than nb
rb are worse than wa
ib are worse than da
ob are worse than ra
sb are worse than wa
nb are worse than sb
jb are worse than za
gb are worse than xa
ka are worse than pb
ob are worse than ba
ca are worse than ub
ya are worse than nb
za are worse than wa
ua are worse than sa
va are worse than ib
ma are worse than qb
xa are worse than ha
pa are worse than ka
la are worse than fa
db are worse than fa
db are worse than va
ba are worse than qa
ub are worse than jb
pb are worse than ia
bb are worse than hb
fb are worse than kb
ua are worse than ga
ia are worse than qb
ya are worse than eb
cb are worse than mb
wa are worse than ea
ub are worse than nb
sb are worse than tb
