200 200
jc are worse than ld
qc are worse than fd
qc are worse than hd
jb are worse than od
id are worse than gb
wc are worse than wa
fb are worse than dd
ta are worse than ja
cb are worse than rd
dd are worse than wc
qb are worse than ub
vd are worse than wc
uc are worse than jd
fb are worse than jc
md are worse than db
fc are worse than da
ta are worse than za
id are worse than nb
kc are worse than wa
fb are worse than xb
ga are worse than fc
tc are worse than lb
mb are worse than nd
vd are worse than sa
mb are worse than cd
pd are worse than dd
bd are worse than sb
dc are worse than zb
fb are worse than nb
mc are worse than vc
ua are worse than va
yc are worse than nb
vd are worse than pa
eb are worse than jc
ld are worse than na
ka are worse than rd
sc are worse than ib
gb are worse than ja
zc are worse than fb
bb are worse than pc
mc are worse than uc
wb are worse than rb
qa are worse than ec
ub are worse than aa
vb are worse than ka
za are worse than pc
gb are worse than ca
hc are worse than fb
ra are worse than pb
sb are worse than ga
dc are worse than ha
bc are worse than zc
pb are worse than hd
id are worse than sc
rc are worse than oc
lb are worse than hb
cb are worse than ha
qd are worse than wc
ha are worse than pd
ma are worse than qc
id are worse than xa
nc are worse than sd
na are worse than uc
da are worse than oc
xb are worse than sd
ea are worse than kb
oc are worse than vc
ca are worse than dc
ga are worse than od
pd are worse than ib
vd are worse than nb
ic are worse than gc
gb are worse than sb
rb are worse than tb
nc are worse than dd
qc are worse than qa
ld are worse than hd
ma are worse than ca
fc are worse than pc
va are worse than ha
lc are worse than za
rd are worse than dd
yb are worse than xb
lb are worse than ia
pb are worse than eb
qb are worse than pd
sc are worse than cc
da are worse than cc
vd are worse than lc
cd are worse than ua
cd are worse than va
xa are worse than pa
ma are worse than sd
wc are worse than bb
ja are worse than xa
sa are worse than ub
fb are worse than la
cb are worse than za
ea are worse than ud
bd are worse than ia
bc are worse than eb
ga are worse than ea
tb are worse than mb
yc are worse than zc
hc are worse than ib
wa are worse than jb
xb are worse than id
fd are worse than xa
na are worse than fc
sb are worse than wa
cd are worse than kc
ua are worse than mc
vd are worse than va
md are worse than ea
zb are worse than sd
ja are worse than sd
jd are worse than lb
wc are worse than ub
tc are worse than wb
ib are worse than ra
wc are worse than nd
wa are worse than ga
fa are worse than qd
rd are worse than mc
jc are worse than rd
hd are worse than zb
vd are worse than kd
vd are worse than ic
yb are worse than ka
hd are worse than vc
ka are worse than sa
ub are worse than sb
lb are worse than sa
ua are worse than qb
vc are worse than ic
dc are worse than rb
lb are worse than ga
fb are worse than lb
ta are worse than ua
xc are worse than va
eb are worse than ic
qd are worse than pd
fb are worse than ob
ld are worse than sa
id are worse than hb
gb are worse than xc
da are worse than ea
rc are worse than qc
cb are worse than fc
dd are worse than jb
pd are worse than gd
ea are worse than jb
wb are worse than cd
uc are worse than rc
xb are worse than ka
cc are worse than hb
ad are worse than kb
ca are worse than gc
pd are worse than fd
dd are worse than oa
dc are worse than hd
ca are worse than ob
ya are worse than yb
dd are worse than td
ya are worse than eb
ac are worse than tc
pb are worse than ga
db are worse than id
ld are worse than wc
gd are worse than sd
mc are worse than nc
ma are worse than bb
oa are worse than kb
ib are worse than vb
hb are worse than la
ad are worse than pd
md are worse than jb
vd are worse than qd
fb are worse than pb
na are worse than pa
ba are worse than kb
qa are worse than nc
ba are worse than xb
eb are worse than jd
ma are worse than ic
gb are worse than cc
pb are worse than gc
id are worse than ib
ja are worse than vd
yb are worse than vd
db are worse than jb
cc are worse than aa
nb are worse than lb
gc are worse than ec
tb are worse than aa
vd are worse than rc
qd are worse than oc
od are worse than td
wb are worse than pa
bc are worse than xc
sd are worse than tb
ma are worse than jb
xb are worse than jc
ha are worse than hc
rc are worse than ac
hc are worse than jb
sa are worse than la
ea are worse than vd
td are worse than ic
qd are worse than ib
ra are worse than kd
ba are worse than ka
lb are worse than xb
za are worse than ga
td are worse than fb
dd are worse than pd
cb are worse than tb
cd are worse than db
ga are worse than jd
kd are worse than yc
od are worse than aa
lc are worse than ka
la are worse than ua
jc are worse than ic
cd are worse than ia
qd are worse than xb
wb are worse than vb
la are worse than id
td are worse than ld
sb are worse than ub
ua are worse than za
nd are worse than aa
wa are worse than tc
db are worse than cb
la are worse than rc
ec are worse than da
rc are worse than uc
nc are worse than gb
od are worse than ad
od are worse than ya
id are worse than jd
vc are worse than jd
id are worse than lb
vc are worse than fd
ab are worse than ra
dd are worse than ud
qd are worse than uc
ud are worse than qa
sb are worse than bd
sd are worse than dc
wb are worse than fa
ka are worse than sc
zc are worse than bb
cc are worse than rc
fa are worse than vb
tb are worse than sb
id are worse than sa
fd are worse than uc
dc are worse than ec
ia are worse than dd
za are worse than pb
fb are worse than tc
oa are worse than db
nb are worse than ka
ic are worse than jd
nc are worse than ja
od are worse than bb
fb are worse than va
cc are worse than hd
qb are worse than la
gd are worse than jd
ha are worse than wa
gc are worse than md
vb are worse than nc
ud are worse than dc
vd are worse than xc
sd are worse than eb
ub are worse than kc
ma are worse than hd
vc are worse than ld
hb are worse than ra
qa are worse than mc
nd are worse than wa
ca are worse than hd
id are worse than mb
vc are worse than tb
jc are worse than fd
kd are worse than ia
wb are worse than id
pa are worse than vd
kc are worse than lc
ab are worse than qb
ub are worse than yb
oc are worse than tc
ob are worse than aa
ob are worse than ia
qa are worse than fb
fd are worse than sa
ra are worse than rc
fc are worse than qb
oa are worse than sc
za are worse than ca
eb are worse than za
pb are worse than td
za are worse than dc
ic are worse than ha
pa are worse than mb
ba are worse than jc
pb are worse than pc
gd are worse than dd
pc are worse than za
pb are worse than kb
ec are worse than ya
cc are worse than za
tb are worse than lc
pc are worse than ma
hd are worse than sb
oa are worse than pa
wa are worse than ha
ma are worse than vd
kc are worse than za
ga are worse than eb
pa are worse than hc
nd are worse than ec
ad are worse than cb
ld are worse than ud
bc are worse than fb
ib are worse than jc
dc are worse than kd
jd are worse than bc
ea are worse than ga
nc are worse than pb
zb are worse than fb
ja are worse than vc
nb are worse than va
ya are worse than bc
ud are worse than eb
fd are worse than oa
pb are worse than bb
wa are worse than ta
dc are worse than ma
ra are worse than ic
rb are worse than zb
va are worse than yb
od are worse than ic
da are worse than wb
pc are worse than qc
ld are worse than sc
aa are worse than ud
rb are worse than nd
uc are worse than ta
sd are worse than wa
ba are worse than cd
kd are worse than qb
hc are worse than la
jc are worse than oa
xa are worse than fb
wc are worse than sa
ua are worse than ab
ia are worse than ya
ab are worse than xc
sb are worse than qa
tb are worse than sd
oc are worse than ob
nb are worse than md
hc are worse than kb
na are worse than ic
qa are worse than bc
cc are worse than xc
mc are worse than pc
pb are worse than nb
ud are worse than pc
fc are worse than hb
oa are worse than eb
la are worse than wc
cc are worse than md
ba are worse than yb
gc are worse than ub
vd are worse than ya
pc are worse than xb
wc are worse than gb
jc are worse than qb
hd are worse than pa
rc are worse than yb
fd are worse than kc
pb are worse than hb
db are worse than zb
sa are worse than sd
dd are worse than ed
sc are worse than jd
uc are worse than ya
lb are worse than eb
sc are worse than kb
cc are worse than jc
bb are worse than vc
kc are worse than ba
fa are worse than ya
sd are worse than nd
nb are worse than nc
pb are worse than wc
