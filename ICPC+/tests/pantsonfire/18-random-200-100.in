200 200
uc are worse than qc
qd are worse than ra
db are worse than ma
oc are worse than vd
va are worse than yc
mb are worse than da
sa are worse than bc
rb are worse than wa
cc are worse than ab
wa are worse than za
da are worse than aa
kb are worse than ea
wa are worse than nb
ma are worse than ac
td are worse than ac
wb are worse than wa
uc are worse than qd
nb are worse than ba
hc are worse than gc
ac are worse than cd
ib are worse than md
fd are worse than ac
sb are worse than ab
wc are worse than ud
ea are worse than wb
xc are worse than hc
wb are worse than la
mc are worse than xa
ca are worse than cb
xc are worse than ac
wc are worse than ac
rc are worse than nc
fa are worse than cb
xa are worse than gc
ia are worse than fc
aa are worse than rb
kc are worse than sb
sc are worse than kb
qd are worse than ab
lb are worse than va
id are worse than hd
hd are worse than ca
ic are worse than jd
eb are worse than jb
nb are worse than lc
ka are worse than nc
pa are worse than gb
fb are worse than vc
ic are worse than cc
fd are worse than va
mc are worse than fd
qa are worse than ab
cd are worse than id
oa are worse than ab
na are worse than zb
wb are worse than zb
ld are worse than hd
bc are worse than xb
uc are worse than fd
lb are worse than mc
ub are worse than vd
db are worse than oa
fb are worse than ma
nb are worse than sb
pc are worse than wa
nc are worse than fb
fc are worse than dd
ja are worse than ld
ua are worse than xa
qc are worse than hd
bb are worse than qc
kb are worse than qa
bc are worse than pb
ra are worse than zc
ya are worse than ga
pb are worse than tc
ga are worse than xa
jb are worse than yc
dd are worse than cc
yb are worse than ud
lb are worse than hc
gb are worse than ub
xb are worse than ea
wa are worse than ra
pb are worse than xb
ra are worse than md
sc are worse than id
pc are worse than qd
pd are worse than ja
ra are worse than vd
gd are worse than ga
kd are worse than tc
ed are worse than sc
ad are worse than gd
ua are worse than pa
sb are worse than ac
oa are worse than pd
ia are worse than kb
kc are worse than xb
lb are worse than vd
ad are worse than cc
ld are worse than wb
od are worse than ia
rd are worse than hd
ra are worse than mc
kb are worse than bc
tc are worse than za
zc are worse than fa
vb are worse than dc
md are worse than ta
wa are worse than ib
ya are worse than kb
bd are worse than gb
ya are worse than pd
ed are worse than jc
sb are worse than ma
pc are worse than hd
na are worse than ic
pd are worse than ic
td are worse than lc
ya are worse than id
qa are worse than rd
la are worse than tb
ba are worse than ka
db are worse than xc
mb are worse than kd
ba are worse than uc
xb are worse than tb
gd are worse than rc
eb are worse than xb
dd are worse than xa
ja are worse than ad
mb are worse than ib
mc are worse than dc
dd are worse than bb
ac are worse than ib
ld are worse than fa
zc are worse than tc
cd are worse than ga
id are worse than hb
ma are worse than xa
cc are worse than qd
uc are worse than jb
sc are worse than ib
pa are worse than ba
ja are worse than qc
kb are worse than zb
vc are worse than na
wa are worse than fa
uc are worse than ud
aa are worse than id
rc are worse than sa
ab are worse than xa
ka are worse than jb
rd are worse than fa
fc are worse than la
ga are worse than ta
fb are worse than qb
sc are worse than ec
na are worse than cd
qb are worse than na
sa are worse than ha
td are worse than uc
nd are worse than ld
pb are worse than ec
od are worse than zb
rb are worse than db
bd are worse than mb
dc are worse than gb
jd are worse than zc
ob are worse than rd
bb are worse than mc
pc are worse than ub
jb are worse than na
yc are worse than cb
rc are worse than ua
yb are worse than hc
da are worse than kb
vb are worse than ea
db are worse than dc
ld are worse than ra
fd are worse than sb
sc are worse than ca
sa are worse than ma
cd are worse than fa
cd are worse than oc
pc are worse than ya
fb are worse than ic
nc are worse than mc
sa are worse than za
ab are worse than kd
da are worse than gc
ha are worse than wa
ld are worse than ha
ic are worse than ma
ia are worse than pc
dc are worse than ab
sb are worse than hb
oc are worse than xa
ba are worse than zb
ua are worse than ib
rc are worse than xc
sb are worse than qd
qa are worse than cc
ac are worse than xa
yc are worse than pb
ha are worse than nc
wb are worse than ua
eb are worse than ec
eb are worse than uc
wc are worse than lb
yb are worse than sa
tc are worse than qc
pb are worse than ya
ta are worse than jc
ka are worse than kd
jb are worse than fa
jc are worse than jd
db are worse than mb
cd are worse than kd
dc are worse than dd
ic are worse than hd
lb are worse than ya
od are worse than ta
sd are worse than oc
fc are worse than ud
gd are worse than cd
oc are worse than za
ec are worse than id
nb are worse than pc
ab are worse than cd
wc are worse than na
hd are worse than jd
ba are worse than oc
za are worse than hb
ha are worse than dd
oc are worse than ta
sa are worse than ec
sb are worse than la
xc are worse than od
zc are worse than gd
nd are worse than oa
qa are worse than fc
nb are worse than vc
rd are worse than eb
od are worse than wa
ka are worse than sa
jd are worse than wb
fa are worse than ob
oc are worse than fd
wc are worse than za
mc are worse than ob
tb are worse than ib
sd are worse than pb
tb are worse than ya
hb are worse than xc
ic are worse than ya
ld are worse than zc
ib are worse than cd
yb are worse than db
lc are worse than qd
da are worse than vc
ma are worse than nd
vc are worse than rc
fb are worse than od
ac are worse than fc
ra are worse than qc
yb are worse than oa
da are worse than ud
dc are worse than aa
wa are worse than pb
xc are worse than fd
ra are worse than qa
ic are worse than zb
td are worse than kc
hb are worse than ic
ia are worse than kd
hd are worse than ua
fb are worse than la
pb are worse than pa
dc are worse than qd
uc are worse than ya
kc are worse than wa
nb are worse than nc
va are worse than qd
sa are worse than rd
jd are worse than pd
ad are worse than fa
kc are worse than md
ec are worse than hc
ea are worse than pb
tc are worse than ma
eb are worse than ia
gd are worse than jc
tc are worse than ab
yc are worse than ab
ua are worse than ba
zb are worse than ib
xa are worse than md
ac are worse than sd
kb are worse than sc
sb are worse than ya
pa are worse than wa
dd are worse than zc
nc are worse than lb
kd are worse than rc
bd are worse than la
ib are worse than jc
ua are worse than sa
wa are worse than rb
sd are worse than zb
sc are worse than qa
ac are worse than vc
da are worse than bb
pa are worse than dc
yc are worse than da
za are worse than ua
bd are worse than ob
qd are worse than ic
sc are worse than hc
ud are worse than gc
dd are worse than db
sc are worse than pc
md are worse than qb
hd are worse than mc
hd are worse than ed
pa are worse than ha
yb are worse than ta
cd are worse than bc
vb are worse than rd
ia are worse than xa
vd are worse than hd
rb are worse than eb
ad are worse than lb
nd are worse than wb
xb are worse than ld
gc are worse than eb
sc are worse than cb
td are worse than ma
yc are worse than ia
qc are worse than sb
pa are worse than ta
lc are worse than wb
ya are worse than kc
rb are worse than hd
hd are worse than qd
wa are worse than cd
za are worse than aa
sa are worse than qc
aa are worse than pc
ja are worse than la
xb are worse than bd
nd are worse than ud
rb are worse than oa
gb are worse than tc
wa are worse than ba
pa are worse than ld
uc are worse than fb
lb are worse than sd
pd are worse than hb
hb are worse than fb
bd are worse than ec
la are worse than ea
ia are worse than db
uc are worse than jc
wb are worse than dd
uc are worse than td
ma are worse than lc
rd are worse than dc
zc are worse than cd
bb are worse than zb
dd are worse than qd
pb are worse than fa
za are worse than ad
bc are worse than tc
lc are worse than jd
cc are worse than rb
rb are worse than nc
vd are worse than da
vb are worse than uc
mc are worse than rd
fa are worse than kb
tb are worse than sc
aa are worse than od
zc are worse than db
ya are worse than ea
dd are worse than fc
zb are worse than wa
kc are worse than cb
ac are worse than qa
zb are worse than qa
da are worse than fa
hc are worse than ud
tc are worse than gc
ba are worse than sa
ed are worse than sa
nc are worse than ob
cc are worse than kc
kc are worse than ga
ha are worse than nb
